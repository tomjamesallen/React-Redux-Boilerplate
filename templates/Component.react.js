import React from 'react'

var {{COMPONENT}} = React.createClass({

  render() {
    return (
      <div>
        <h2>{{COMPONENT}}</h2>
      </div>
    )
  }
})

{{CSS_FILE_COMMENT}}

export default {{COMPONENT}}
